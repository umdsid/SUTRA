{
  "hierarchy_rerun": false,
  "scope": "Level-0 measured contact edges",
  "communication_support_positive_quantile": 0.1,
  "channel_flux": "sqrt(sender_expression * receiver_expression)",
  "metrics": {
    "signed_net": "sum(delta_r)/sum(weight_r)",
    "net_asymmetry": "abs(sum(delta_r))/sum(weight_r)",
    "directional_content": "sum(abs(delta_r))/sum(weight_r)",
    "cancellation": "1-abs(sum(delta_r))/sum(abs(delta_r))"
  },
  "interaction_classes": [
    "contact",
    "diffusible"
  ],
  "outputs": {
    "edge_level": true,
    "interaction_level": true,
    "figure_ready": true
  },
  "next_stage": "direction-dependent local metric preflight"
}
