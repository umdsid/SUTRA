# Source-only transfer package

This is the same RC2 canonical code architecture, packaged without bulky rendered
PNG/PDF panels and movie files so it can be downloaded reliably.

Use it to replace the code in the existing RC1 Git checkout. Existing frozen
paper products already present in RC1 can remain in place.
